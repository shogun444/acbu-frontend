name: Frontend QA

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  quality-gate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run typecheck
        run: pnpm typecheck

      - name: Run dependency audit
        run: pnpm audit --audit-level=critical --audit-level=moderate || true

      - name: Run lint
        run: pnpm lint

      - name: Run build
        run: pnpm build
        env:
          # Prevent Next.js telemetry during CI
          NEXT_TELEMETRY_DISABLED: 1
          # Stub required public env vars so the build succeeds without secrets
          NEXT_PUBLIC_API_URL: https://api.acbu.io
          NEXT_PUBLIC_BILLS_ENABLED: false

      - name: Run component tests
        run: pnpm test

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium

      - name: Run E2E smoke tests
        run: pnpm test:e2e
        env:
          NEXT_TELEMETRY_DISABLED: 1
